apiVersion: v1
kind: Service

metadata:
  name: active-service
  namespace: bluegreen-demo

  labels:
    app: bluegreen-app

spec:
  selector:
    app: bluegreen-app

  ports:
    - name: http
      port: 80
      targetPort: 3000

    - name: metrics
      port: 9090
      targetPort: 3000