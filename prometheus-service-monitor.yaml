apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: bluegreen-monitor
  namespace: monitoring
  labels:
    release: prometheus

spec:
  selector:
    matchLabels:
      app: bluegreen-app

  namespaceSelector:
    matchNames:
      - bluegreen-demo

  endpoints:
    - port: metrics
      path: /metrics
      interval: 15s